'use strict';

/**
 * @ngdoc module
 * @name feedReaderApp
 * @description 
 * Feed reader application. It uses the Google
 * Feed Reader API to grab RSS feeds as JSON object.
 */
var appName = 'feedReaderApp';

angular
    .module(appName, []);



